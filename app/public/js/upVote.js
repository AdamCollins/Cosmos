function incrementPostScore(postId, target){
  // var stars = $(this).attr('post_id',postId)
  // var starCount = stars.text();
  // if(!starCount)
  //   stars.text('ADDED 1 HOUR');
  // else{
  //   stars.text('ADDED ' + stars.text().match(/\d+/)[0]+' HOURS')
  // }
  console.log(target)

}
